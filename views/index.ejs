<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Test</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
            <input name="country" type="text" class="ghost-input" placeholder="Enter Country" required>
          <input name="city" type="text" class="ghost-input" placeholder="Enter City" required>
          <input name="street" type="text" class="ghost-input" placeholder="street" required>
          <input name="housenumber" type="text" class="ghost-input" placeholder="Enter housenumber" required>
          <!-- <input name="zip" type="text" class="ghost-input" placeholder="Enter zipcode" required> -->
          <input type="submit" class="ghost-button" value="Get ans">
        </form>
        <% if(location !== null){ %>
          <p><%= location %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>